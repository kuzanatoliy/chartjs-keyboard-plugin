import { ChartjsKeyboardPluginEngine } from './chartjs-keyboard-plugin-engine';

describe('ChartjsKeyboardPluginEngine', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(ChartjsKeyboardPluginEngine).toBeDefined();
  });
});

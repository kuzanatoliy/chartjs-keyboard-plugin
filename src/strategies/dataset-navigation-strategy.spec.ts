import { DataSetNavigationStrategy } from './dataset-navigation-strategy';

describe('DataSetNavigationStrategy', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(DataSetNavigationStrategy).toBeDefined();
  });
});

import { DataFirstNavigationStrategy } from './data-first-navigation-strategy';

describe('DataFirstNavigationStrategy', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(DataFirstNavigationStrategy).toBeDefined();
  });
});

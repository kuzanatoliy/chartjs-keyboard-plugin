import { DataNavigationStrategy } from './data-navigation-strategy';

describe('DataNavigationStrategy', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(DataNavigationStrategy).toBeDefined();
  });
});

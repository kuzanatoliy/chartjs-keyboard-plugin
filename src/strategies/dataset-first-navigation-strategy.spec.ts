import { DataSetFirstNavigationStrategy } from './dataset-first-navigation-strategy';

describe('DataSetFirstNavigationStrategy', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(DataSetFirstNavigationStrategy).toBeDefined();
  });
});
